# Keep LF on files that run on the Linux host — CRLF would break shell scripts
# and systemd unit values (e.g. ExecStart).
*.sh      text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf
*.service text eol=lf
*.timer   text eol=lf
